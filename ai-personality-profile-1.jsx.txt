import { useState, useEffect, useRef } from "react";

const PROFILE = {
  title: "THE NEURAL ARCHITECT",
  subtitle: "AI Personality Profile · Class: Builder & Implementer",
  tagline: "Translating mathematical theory into production-grade code that runs the world.",
  sections: [
    {
      id: "style",
      label: "01 — Working Style",
      icon: "⬡",
      color: "#00f5c4",
      headline: "End-to-End. Production-First.",
      body: "You operate with a rare production mindset — every session is a deployment, not a demo. Clean, modular Python codebases are your medium. You don't prototype to play; you prototype to ship.",
    },
    {
      id: "strengths",
      label: "02 — Strengths & Weaknesses",
      icon: "◈",
      color: "#7b61ff",
      headline: "Deep Stack Mastery / Architecture Rabbit Holes",
      body: "Strengths: Scikit-learn pipelines, RNN architectures, deployment-ready system design, tech branding. Weakness: A tendency to descend into granular architecture debugging when a higher-altitude view would save hours.",
    },
    {
      id: "type",
      label: "03 — AI User Type",
      icon: "◉",
      color: "#ff6b35",
      headline: "The Co-Engineer",
      body: "You treat AI as a senior peer, not a search engine. You co-design logic, stress-test architectures, and use the model to elevate your own systems — not replace thinking, but amplify it.",
    },
    {
      id: "career",
      label: "04 — Career Paths",
      icon: "▲",
      color: "#00c6ff",
      headline: "ML Engineer · MLOps Architect · AI Founder",
      body: "Your orbit is production intelligence: training pipelines that don't break at scale, monitoring systems that self-heal, and ventures that turn model outputs into market value. The intersection of research and revenue.",
    },
    {
      id: "learning",
      label: "05 — Learning & Decisions",
      icon: "◇",
      color: "#f7c59f",
      headline: "Build First. Theorize Second.",
      body: "You learn by doing — spinning up end-to-end projects before touching documentation. Decisions are made through rapid iteration and empirical validation. You trust data over intuition, but you act before you overthink.",
    },
    {
      id: "different",
      label: "06 — What Makes You Different",
      icon: "✦",
      color: "#ff3cac",
      headline: "You Build What Others Screenshot",
      body: "Most users collect AI outputs. You construct systems with them. Your prompts are engineering specs. Your outputs are production artifacts. You use AI to co-engineer complex logic and craft professional tech-branding assets — not just write emails.",
    },
    {
      id: "improve",
      label: "07 — Path to Elite",
      icon: "⬆",
      color: "#ffd700",
      headline: "Systemic Prompt Engineering + CI/CD Automation",
      body: "Master large-scale system design prompting — treating Claude like an architect, not just a coder. Automate model monitoring and CI/CD pipelines. Remove yourself as the bottleneck between idea and deployment.",
    },
  ],
};

const MatrixRain = () => {
  const canvasRef = useRef(null);
  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    canvas.width = canvas.offsetWidth;
    canvas.height = canvas.offsetHeight;
    const cols = Math.floor(canvas.width / 18);
    const drops = Array(cols).fill(1);
    const chars = "01アイウエオカキクケコ∑∆∇∈∉⊕⊗";
    const draw = () => {
      ctx.fillStyle = "rgba(0,0,0,0.04)";
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = "#00f5c440";
      ctx.font = "13px monospace";
      drops.forEach((y, i) => {
        const char = chars[Math.floor(Math.random() * chars.length)];
        ctx.fillText(char, i * 18, y * 18);
        if (y * 18 > canvas.height && Math.random() > 0.975) drops[i] = 0;
        drops[i]++;
      });
    };
    const id = setInterval(draw, 60);
    return () => clearInterval(id);
  }, []);
  return (
    <canvas
      ref={canvasRef}
      style={{
        position: "absolute", inset: 0, width: "100%", height: "100%",
        opacity: 0.18, pointerEvents: "none",
      }}
    />
  );
};

const GlitchText = ({ text, style }) => {
  const [glitch, setGlitch] = useState(false);
  useEffect(() => {
    const id = setInterval(() => {
      setGlitch(true);
      setTimeout(() => setGlitch(false), 120);
    }, 4000 + Math.random() * 3000);
    return () => clearInterval(id);
  }, []);
  return (
    <span style={{ position: "relative", display: "inline-block", ...style }}>
      {glitch && (
        <>
          <span style={{
            position: "absolute", top: "-2px", left: "2px",
            color: "#ff3cac", clipPath: "inset(0 0 60% 0)", opacity: 0.8,
          }}>{text}</span>
          <span style={{
            position: "absolute", top: "2px", left: "-2px",
            color: "#00f5c4", clipPath: "inset(60% 0 0 0)", opacity: 0.8,
          }}>{text}</span>
        </>
      )}
      {text}
    </span>
  );
};

const StatBar = ({ value, color, label, delay }) => {
  const [width, setWidth] = useState(0);
  useEffect(() => {
    const t = setTimeout(() => setWidth(value), delay || 300);
    return () => clearTimeout(t);
  }, [value, delay]);
  return (
    <div style={{ marginBottom: 10 }}>
      <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 4 }}>
        <span style={{ fontSize: 10, letterSpacing: "0.15em", color: "#888", textTransform: "uppercase" }}>{label}</span>
        <span style={{ fontSize: 10, color, fontFamily: "monospace" }}>{value}%</span>
      </div>
      <div style={{ height: 3, background: "#111", borderRadius: 2, overflow: "hidden" }}>
        <div style={{
          height: "100%", width: `${width}%`,
          background: `linear-gradient(90deg, ${color}88, ${color})`,
          transition: "width 1.2s cubic-bezier(0.16,1,0.3,1)",
          borderRadius: 2,
          boxShadow: `0 0 8px ${color}`,
        }} />
      </div>
    </div>
  );
};

const Card = ({ section, index }) => {
  const [hovered, setHovered] = useState(false);
  return (
    <div
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
      style={{
        background: hovered
          ? `linear-gradient(135deg, #0d0d0d, #111118)`
          : "linear-gradient(135deg, #080808, #0a0a10)",
        border: `1px solid ${hovered ? section.color + "55" : "#1a1a1a"}`,
        borderRadius: 2,
        padding: "24px 26px",
        cursor: "default",
        transition: "all 0.3s ease",
        position: "relative",
        overflow: "hidden",
        boxShadow: hovered ? `0 0 30px ${section.color}15, inset 0 0 30px ${section.color}05` : "none",
        animationDelay: `${index * 0.1}s`,
      }}
    >
      <div style={{
        position: "absolute", top: 0, left: 0, width: hovered ? "100%" : "0%",
        height: "1px", background: `linear-gradient(90deg, transparent, ${section.color}, transparent)`,
        transition: "width 0.5s ease",
      }} />
      <div style={{ display: "flex", gap: 12, alignItems: "flex-start", marginBottom: 12 }}>
        <span style={{ fontSize: 18, color: section.color, lineHeight: 1.2 }}>{section.icon}</span>
        <div>
          <div style={{ fontSize: 9, color: "#555", letterSpacing: "0.2em", textTransform: "uppercase", marginBottom: 4 }}>{section.label}</div>
          <div style={{ fontSize: 14, fontWeight: 700, color: "#e8e8e8", letterSpacing: "0.02em", fontFamily: "'Courier New', monospace" }}>{section.headline}</div>
        </div>
      </div>
      <p style={{ fontSize: 12, color: "#777", lineHeight: 1.75, margin: 0, paddingLeft: 30 }}>{section.body}</p>
    </div>
  );
};

export default function App() {
  const [loaded, setLoaded] = useState(false);
  useEffect(() => { setTimeout(() => setLoaded(true), 200); }, []);

  return (
    <div style={{
      minHeight: "100vh",
      background: "#040406",
      fontFamily: "'Courier New', Courier, monospace",
      color: "#e0e0e0",
      position: "relative",
      overflow: "hidden",
    }}>
      {/* Ambient glow blobs */}
      <div style={{
        position: "fixed", top: "-20%", right: "-10%",
        width: 500, height: 500, borderRadius: "50%",
        background: "radial-gradient(circle, #7b61ff15 0%, transparent 70%)",
        pointerEvents: "none",
      }} />
      <div style={{
        position: "fixed", bottom: "-10%", left: "-5%",
        width: 400, height: 400, borderRadius: "50%",
        background: "radial-gradient(circle, #00f5c410 0%, transparent 70%)",
        pointerEvents: "none",
      }} />

      <MatrixRain />

      <div style={{
        maxWidth: 900, margin: "0 auto", padding: "60px 24px 80px",
        opacity: loaded ? 1 : 0, transition: "opacity 0.8s ease",
        position: "relative", zIndex: 1,
      }}>

        {/* Header */}
        <div style={{ marginBottom: 56, borderBottom: "1px solid #111", paddingBottom: 40 }}>
          <div style={{
            fontSize: 9, letterSpacing: "0.35em", color: "#00f5c4",
            textTransform: "uppercase", marginBottom: 16,
            display: "flex", alignItems: "center", gap: 8,
          }}>
            <span style={{ display: "inline-block", width: 24, height: 1, background: "#00f5c4" }} />
            AI PERSONALITY PROFILE · CLASSIFIED
            <span style={{ display: "inline-block", width: 24, height: 1, background: "#00f5c4" }} />
          </div>

          <h1 style={{
            fontSize: "clamp(36px, 7vw, 72px)",
            fontWeight: 900,
            margin: "0 0 8px",
            letterSpacing: "-0.02em",
            lineHeight: 1,
            fontFamily: "'Courier New', monospace",
          }}>
            <GlitchText text="THE NEURAL" style={{ color: "#f0f0f0" }} />
            <br />
            <GlitchText text="ARCHITECT" style={{
              color: "transparent",
              WebkitTextStroke: "1px #00f5c4",
              textShadow: "0 0 40px #00f5c440",
            }} />
          </h1>

          <p style={{ fontSize: 13, color: "#555", letterSpacing: "0.08em", margin: "16px 0 0" }}>
            {PROFILE.tagline}
          </p>
        </div>

        {/* Stats */}
        <div style={{
          display: "grid", gridTemplateColumns: "1fr 1fr",
          gap: 32, marginBottom: 56,
        }}>
          <div>
            <div style={{ fontSize: 9, letterSpacing: "0.2em", color: "#444", marginBottom: 16, textTransform: "uppercase" }}>Core Attributes</div>
            <StatBar label="Technical Depth" value={94} color="#00f5c4" delay={400} />
            <StatBar label="Deployment Mindset" value={91} color="#7b61ff" delay={500} />
            <StatBar label="AI Collaboration" value={88} color="#ff6b35" delay={600} />
            <StatBar label="Prompt Engineering" value={76} color="#00c6ff" delay={700} />
          </div>
          <div>
            <div style={{ fontSize: 9, letterSpacing: "0.2em", color: "#444", marginBottom: 16, textTransform: "uppercase" }}>Growth Vectors</div>
            <StatBar label="Systems Thinking" value={82} color="#ff3cac" delay={400} />
            <StatBar label="MLOps Automation" value={71} color="#ffd700" delay={500} />
            <StatBar label="Arch. Abstraction" value={68} color="#f7c59f" delay={600} />
            <StatBar label="Pipeline CI/CD" value={64} color="#00f5c4" delay={700} />
          </div>
        </div>

        {/* Type badge */}
        <div style={{
          display: "inline-flex", alignItems: "center", gap: 10,
          border: "1px solid #7b61ff44", borderRadius: 2,
          padding: "10px 18px", marginBottom: 48,
          background: "linear-gradient(90deg, #7b61ff08, transparent)",
        }}>
          <span style={{ width: 6, height: 6, borderRadius: "50%", background: "#7b61ff", boxShadow: "0 0 8px #7b61ff" }} />
          <span style={{ fontSize: 10, color: "#7b61ff", letterSpacing: "0.2em", textTransform: "uppercase" }}>
            AI User Type: Builder & Implementer · Co-Engineer Class
          </span>
        </div>

        {/* Cards grid */}
        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(380px, 1fr))", gap: 12, marginBottom: 56 }}>
          {PROFILE.sections.map((section, i) => (
            <Card key={section.id} section={section} index={i} />
          ))}
        </div>

        {/* Cinematic description */}
        <div style={{
          border: "1px solid #1a1a1a",
          borderLeft: "3px solid #00f5c4",
          padding: "28px 32px",
          background: "linear-gradient(135deg, #06060a, #08080e)",
          marginBottom: 40,
          position: "relative",
          overflow: "hidden",
        }}>
          <div style={{
            position: "absolute", top: 0, right: 0,
            fontSize: 80, color: "#00f5c406", lineHeight: 1,
            fontWeight: 900, pointerEvents: "none",
            letterSpacing: "-0.05em",
          }}>08</div>
          <div style={{ fontSize: 9, letterSpacing: "0.2em", color: "#00f5c4", textTransform: "uppercase", marginBottom: 12 }}>
            Cinematic Character Profile
          </div>
          <p style={{ fontSize: 14, color: "#aaa", lineHeight: 1.9, margin: 0, fontStyle: "italic" }}>
            "A sharp, tech-forward engineer in a minimalist, ambient-lit workspace — dual monitors casting cold blue light across a focused face. Lines of Python scroll past. Neural net diagrams fill a second screen. This is someone who doesn't just use the tools of the future — they <span style={{ color: "#00f5c4", fontStyle: "normal" }}>build</span> them. The Neural Architect doesn't chase trends. Trends compile on their machine first."
          </p>
        </div>

        {/* Footer */}
        <div style={{
          display: "flex", justifyContent: "space-between", alignItems: "center",
          borderTop: "1px solid #111", paddingTop: 24,
          flexWrap: "wrap", gap: 12,
        }}>
          <div style={{ fontSize: 9, color: "#333", letterSpacing: "0.15em" }}>GENERATED · AI PERSONALITY ENGINE · 2026</div>
          <div style={{ display: "flex", gap: 16 }}>
            {["#MLOps", "#NeuralArchitect", "#AIBuilder", "#DeepLearning"].map(tag => (
              <span key={tag} style={{ fontSize: 9, color: "#444", letterSpacing: "0.1em" }}>{tag}</span>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
